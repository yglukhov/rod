# This is needed to include the components into the compilation so they
# register themselves

import animation.animated_mesh, particle_system, particle_helpers, camera, channel_levels, mesh_component,
    overlay, particle_emitter, solid,
    sprite, text_component, light, clipping_rect_component,
    blur_component, visual_modifier, node_selector, tracer
